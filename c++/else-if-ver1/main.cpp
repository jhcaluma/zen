#include <iostream>
using namespace std;

int main() {
    int num1 = 50;
    
    if(x == 1)
        cout<<"It is time to read a book.";
    else if(x == 2)
        cout<<"It is time for lunch.";
}